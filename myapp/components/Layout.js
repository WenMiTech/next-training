import { defaultProps } from "../node_modules/antd-mobile/lib/search-bar/PropsType";

const Layout = props => {
    return (
        <div>
            {props.children}
        </div>
    )
}

export default Layout;